<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gopair.roomservice.mapper.RoomMapper">

    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        room_id, room_code, room_name, description, max_members, current_members,
        owner_id, status, expire_time, version,
        password_mode, password_hash, password_visible,
        create_time, update_time
    </sql>

    <!-- 根据房间码查询房间 -->
    <select id="selectByRoomCode" resultType="com.gopair.roomservice.domain.po.Room">
        SELECT 
            <include refid="Base_Column_List" />
        FROM room 
        WHERE room_code = #{roomCode}
        AND status = 0
    </select>

    <!-- 查询过期房间列表（定时任务废弃，仅保留兼容） -->
    <select id="selectExpiredRooms" resultType="com.gopair.roomservice.domain.po.Room">
        SELECT
            <include refid="Base_Column_List" />
        FROM room
        WHERE expire_time <![CDATA[ < ]]> #{currentTime}
        AND status = 0
        ORDER BY expire_time ASC
        LIMIT 100
    </select>

    <!-- 查询需要清理的房间：已关闭超过指定阈值时间 -->
    <select id="selectRoomsToClean" resultType="com.gopair.roomservice.domain.po.Room">
        SELECT
            <include refid="Base_Column_List" />
        FROM room
        WHERE status = 1
          AND closed_time IS NOT NULL
          AND closed_time < #{thresholdTime}
        ORDER BY closed_time ASC
        LIMIT 100
    </select>

    <!-- 根据用户ID查询用户创建的房间列表 -->
    <select id="selectRoomsByOwnerId" resultType="com.gopair.roomservice.domain.po.Room">
        SELECT 
            <include refid="Base_Column_List" />
        FROM room 
        WHERE owner_id = #{ownerId}
        AND status = 0
        ORDER BY create_time DESC
    </select>

    <!-- 更新房间当前成员数（手工CAS，保留兼容） -->
    <update id="updateCurrentMembers">
        UPDATE room 
        SET current_members = #{currentMembers},
            version = version + 1,
            update_time = NOW()
        WHERE room_id = #{roomId}
        AND version = #{version}
    </update>

    <!-- 原子加一（仅在未满时） -->
    <update id="incrementMembersIfNotFull">
        UPDATE room
        SET current_members = current_members + 1,
            version = version + 1,
            update_time = NOW()
        WHERE room_id = #{roomId}
          AND current_members <![CDATA[ < ]]> max_members
    </update>

    <!-- 原子减一（仅在大于0时） -->
    <update id="decrementMembersIfPositive">
        UPDATE room
        SET current_members = current_members - 1,
            version = version + 1,
            update_time = NOW()
        WHERE room_id = #{roomId}
          AND current_members <![CDATA[ > ]]> 0
    </update>

    <!-- 查询用户参与的房间列表（带用户角色、加入时间），支持分页 -->
    <select id="selectUserRoomsPage" resultType="com.gopair.roomservice.domain.vo.RoomVO">
        SELECT r.room_id           AS roomId,
               r.room_code         AS roomCode,
               r.room_name         AS roomName,
               r.description       AS description,
               r.max_members       AS maxMembers,
               r.current_members   AS currentMembers,
               r.owner_id          AS ownerId,
               r.status            AS status,
               r.expire_time       AS expireTime,
               r.create_time       AS createTime,
               r.update_time       AS updateTime,
               r.password_mode     AS passwordMode,
               r.password_visible  AS passwordVisible,
               rm.role             AS userRole,
               rm.join_time        AS joinTime
        FROM room r
        INNER JOIN room_member rm ON r.room_id = rm.room_id AND rm.user_id = #{userId}
        <where>
            <if test="status != null">
                r.status = #{status}
            </if>
        </where>
        ORDER BY r.create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 统计用户参与的房间数量（支持状态过滤） -->
    <select id="countUserRoomsWithRelationship" resultType="java.lang.Long">
        SELECT COUNT(DISTINCT r.room_id)
        FROM room r
        INNER JOIN room_member rm ON r.room_id = rm.room_id AND rm.user_id = #{userId}
        <where>
            <if test="status != null">
                r.status = #{status}
            </if>
        </where>
    </select>

</mapper> 